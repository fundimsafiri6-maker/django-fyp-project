services:
  - type: web
    name: django-fyp-project
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_complaints_system.wsgi:application
    runtime: python-3.11.9